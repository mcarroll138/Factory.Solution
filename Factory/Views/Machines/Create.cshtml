@{
  Layout = "_Layout";
}

@model Factory.Models.Machine

<h4>Add a new machine</h4>

@* <p><strong>NOTE:</strong>You need to have at least one enginee added to your system before you can add a client.</p>
<p>Go to @Html.ActionLink("this page", "Create", "Stylists") to add a new stylist</p> *@

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)

  @* @Html.LabelFor(model => model.Stylist)
@Html.DropDownList("StylistId") *@

  <input type="submit" value="Add new machine" class="btn btn-defualt" />
}
<p>@Html.ActionLink("Show all machines", "Index")</p>