@{
  Layout = "_Layout";
}

@using Factory.Models

<h1>Machinery List</h1>

@if (@Model.Count == 0)
{
  <h3>No Machinery is currently in your system</h3>
}

<ul>
  @foreach (Machine machine in Model)
  {
    <li>@Html.ActionLink($"{machine.Name}", "Details", new { id = machine.MachineId})</li>
  }
</ul>

<p>@Html.ActionLink("Add a new machine", "Create")</p>
<p>@Html.ActionLink("Add a new engineer", "Create", "Engineers")</p>